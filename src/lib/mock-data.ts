
import { Restaurant } from './types';

/**
 * Mock data is now completely cleared.
 * The application starts as a blank slate.
 * Admins must add all content via the Partner Dashboard.
 */
export const RESTAURANTS: Restaurant[] = [];
