
import { Restaurant } from './types';

/**
 * Mock data is now cleared to ensure the app starts fresh.
 * Admins must add restaurants and dishes through the dashboard.
 */
export const RESTAURANTS: Restaurant[] = [];
